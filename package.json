{
  "name": "waffleiron-cms",
  "version": "1.0.0",
  "description": "cms with live edit, flexible fields, custom fields. built with express + htmx + alpinejs + tailwind + sqlite",
  "private": true,
  "author": "waffles",
  "license": "ISC",
  "packageManager": "npm",
  "workspaces": [
    "packages/client",
    "packages/server",
    "packages/theme"
  ],
  "scripts": {
    "dev": "concurrently \"yarn workspace client dev\" \"yarn workspace theme dev\""
  },
  "dependencies": {
    "concurrently": "^8.2.1"
  }
}
